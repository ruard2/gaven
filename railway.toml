[build]
builder = "nixpacks"

[deploy]
startCommand = "npx prisma migrate resolve --rolled-back 0001_init 2>/dev/null; npx prisma migrate deploy && npx next start -p ${PORT:-3000}"
healthcheckPath = "/"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
