[build]
builder = "nixpacks"

[deploy]
startCommand = "npx prisma migrate resolve --rolled-back 0001_init 2>/dev/null; npx prisma migrate deploy && HOSTNAME=0.0.0.0 node .next/standalone/server.js"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
